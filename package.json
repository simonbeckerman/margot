{
  "name": "beckerman-companion",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "smoke": "npm test && node scripts/smoke-mcp.mjs"
  },
  "devDependencies": {
    "typescript": "~5.7.0",
    "vitest": "~3.0.5"
  }
}
